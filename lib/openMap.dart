import 'package:flutter/material.dart';

class openMap extends StatelessWidget {
  const openMap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Emergency button working'),
    );
  }
}
